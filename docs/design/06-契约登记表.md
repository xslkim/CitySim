# WorldSim · 06 契约登记表

> 版本 v1.0 · 2026-09-20
> 上游唯一有效文档：`docs/WorldSim-最终方案.md`（v3.4）。本表是评审《评审建议-2026-09-20》P1-1/P1-2 的仲裁产物，**W1 Schema v1 冻结的唯一依据**：事件类型、字段命名、数字口径以本表为唯一仲裁方。各文档（00~05）引用本表，**不复制全表**；新增事件类型/字段/数值先登记本表再实现。

---

## 1. 事件类型注册表

### 1.1 命名规则与 trigger 枚举

- **类型命名**：`<domain>.<object>[.<verb>]`，全小写，object/verb 用 snake_case。域清单（封闭集，新增域需修订本表）：`time / agent / dialogue / social / world / economy / director / system / state / relation`。
- **trigger 枚举**（6 值，与 04 §5.1 CHECK 一致）：`autonomous | world | director | gift | vote | system`。`gift/vote` 为直播形态预留，阶段 1 无类型使用。观察端过滤枚举必须包含 `system`（评审 P2-17）。
- **source 形态**（04 §5.2）：`'agent:A07'`（agent 域动作）| `'world'` | `'director'` | `'system'`。
- 对话类事件（dialogue 域）`payload.lines[]` 必有，逐句播放时间为 `lines[].at_offset_s`（见 §2 字段表）。
- 干预审计只看 `trigger` 字段不看域：编剧发起的世界事件（如 L0 扰动）类型仍在 `world.*`，但 `trigger='director'` 并落入干预率统计（01 §6.3）。

### 1.2 注册表（全量）

| 类型名 | source | trigger | payload 关键字段 | UI 文案模板（03 §5.3） | 审计域 |
|---|---|---|---|---|---|
| `time.paused` / `time.resumed` | system | system | `reason`、`at_sim` | 待补行（时间轴分隔条样式） | — |
| `time.catchup.start` / `time.catchup.end` | system | system | `mode`、`from_sim`、`to_sim` | 待补行 | — |
| `time.batch_advanced` | system | system | `sim_hours` | 待补行 | — |
| `time.day_summary` | system | system | `day` | 已有行（旧名 `sim.day_summary`） | — |
| `agent.move` | agent | autonomous | `from`、`to`、`sim_cost_min` | 已有行 | consistency（不在两地） |
| `agent.work` | agent | autonomous | `task_id?`、`sim_cost_min` | 待补行 | — |
| `agent.rest` | agent | autonomous | `mode: nap/sleep` | 待补行 | — |
| `agent.eat` | agent | autonomous | `venue`、`with[]`、`amount_cents` | 待补行 | **economy** |
| `agent.shop` | agent | autonomous | `store`、`item_category`、`amount_cents` | 待补行 | **economy** |
| `agent.trade_stock` | agent | autonomous | `symbol`、`side`、`price`、`amount_cents` | 待补行 | **economy** |
| `agent.think` | agent | autonomous | `topic_hint` | 待补行（复用反思灰虚线样式） | — |
| `agent.promoted` / `agent.demoted` | system | system | `from_tier`、`to_tier`、`score 分解`、`gini`、`reason?`、`caused_by?`（后两者为事件驱动升格路径字段，04 §4.2 路径二） | 已有行 | lod |
| `agent.reflection` | agent | autonomous | `text_display` | 已有行 | — |
| `state.needs_delta` | system | autonomous / system | `changes[]:{agent_id, need, delta, new_value, cause:'e<seq>'}`（每 tick 合并，04 §6.5） | 不渲染（internal，仅投影/调试态） | consistency（缓存对账⑥） |
| `relation.changed` | system | autonomous / system | `changes[]:{a_id, b_id, delta_affinity, delta_tension, labels_added?, labels_removed?, cause:'e<seq>'}`（每 tick 合并，04 §6.5） | 不渲染（internal，仅投影/调试态） | consistency（缓存对账⑥、关系周变化率数据源） |
| `dialogue.chat` | agent | autonomous | `participants`、`mode: small/deep`、`topic_ids[]`、`lines[]`、`witnesses[]`、`text_display` | 已有行（旧名 `agent.chat`） | consistency（双人记忆同源） |
| `dialogue.argue` | agent | autonomous | `participants`、`reason_hint`、`lines[]`、`witnesses[]` | 已有行（旧名 `agent.argue`） | consistency |
| `dialogue.gossip` | agent | autonomous | `teller`、`listener`、`about`、`cites[]`、`distortion`、`lines[]` | 已有行（旧名 `agent.gossip`） | consistency |
| `dialogue.confess` | agent | autonomous | `participants`、`result: accepted/rejected`、`lines[]` | 待补行 | consistency |
| `dialogue.apologize` | agent | autonomous | `participants`、`for_event_ref('e<seq>')`、`result`、`lines[]` | 待补行 | consistency |
| `social.send_message` | agent | autonomous | `from`、`to`、`content_hint` | 待补行 | — |
| `social.invite` | agent | autonomous | `from`、`to`、`activity`、`time`、`location` | 已有行（旧名 `agent.invite`） | consistency |
| `social.invite.counter` | agent | autonomous | `from`、`to`、`round: 1/2`、`new_time` | 待补行（01 §5.2 改期独立事件） | consistency |
| `social.refuse` | agent | autonomous | `from`、`to`、`request_ref`、`politeness: 0/1` | 已有行（旧名 `agent.refuse`） | consistency |
| `social.give_gift` | agent | autonomous | `from`、`to`、`tier: 1/2/3`、`amount_cents` | 待补行 | **economy** |
| `social.help` | agent | autonomous | `from`、`to`、`matter` | 待补行 | — |
| `social.borrow_money` | agent | autonomous | `from`、`to`、`amount_cents`、`result` | 待补行 | **economy** |
| `social.repay_money` | agent | autonomous | `from`、`to`、`amount_cents`、`debt_ref` | 待补行（P0-2 新增动作；还款期限 14 模拟日由 01 持有） | **economy** |
| `social.appointment.created` / `.remind` / `.stood_up` | system | system | `participants`、`activity`、`at_sim` | 待补行 | consistency |
| `world.announce` | world | world / director | `title`、`body`、`scope: company/floor/all`、`stock_shock?` | 已有行 | — |
| `world.holiday` | world | world | `name`、`days`、`special_rules` | 待补行 | — |
| `world.team_building` | world | world / director | `dept/all`、`activity`、`amount_cents?`（向 agent 收费时必有） | 待补行 | **economy**（收费时） |
| `world.promotion_window` | world | world | `dept`、`slots`、`candidates[]` | 待补行 | — |
| `world.perf_review` | world | world | `agent_id`、`manager_id`、`grade: S/A/B/C`、`delta_salary?` | 待补行 | **economy**（含 delta_salary 时；实际入账经 `economy.payroll` 结算） |
| `world.company_crisis` | world | world / director | `scope`、`severity` | 待补行 | — |
| `world.disturb.illness` / `.weather` / `.complaint` / `.lucky` | world | world / director | 按 01 §6.1 模板；`lucky` 含金额时 `amount_cents` 必有 | 待补行 | **economy**（仅 lucky 含金额时） |
| `economy.payroll` | world | world | `agent_id`、`amount_cents`、`month` | 已有行（旧名 `world.payday`） | **economy** |
| `economy.bill.rent` / `economy.bill.utility` | world | world | `agent_id`、`amount_cents`、`due?` | 待补行 | **economy** |
| `economy.stock.tick` | world | world | `symbol`、`open`、`close`、`r`、`seed`（无 `amount_cents`，不进余额守恒求和） | 已有行（旧名 `world.stock`） | **economy** |
| `economy.settle` | world / system | world / system | `agent_id`、`amount_cents`、`reason` | 待补行（兜底结算类型，仅无法归类时使用） | **economy** |
| `director.intervene` | director | director | `level: L0/L1/L2`（L3 永禁）、`arc_id?`、`reason` | 已有行 | intervention |
| `system.llm.failover` | system | system | `task_type`、`from_provider`、`to_provider`、`reason` | 待补行（调试态） | — |
| `system.llm.throttle` | system | system | `level: alarm/throttle/recover`、`ratio` | 待补行（调试态） | — |

### 1.3 economy 域前缀对账规则（评审 P1-2 裁决）

1. 凡引起 `agents.balance_cents` 结算的事件类型，必须在本表标记**审计域 = economy**，且金额统一落 `payload.amount_cents`（BIGINT，单位分，正=入账、负=出账）。
2. 纯系统经济事件（无 agent 动作主体）一律 `economy.*` 前缀；agent 动作产生的结算（`agent.eat/shop/trade_stock`、`social.give_gift/borrow_money/repay_money` 等）保留动作域命名，但同样打 economy 审计标记。
3. 审计 SQL（04 §10.1 ①余额守恒）的经济类型清单**由本表 economy 标记生成**（`type IN (...)`），不得再仅凭 `LIKE 'economy.%'` 对账——此前口径会系统性漏掉动作域结算、误报红灯；`LIKE 'economy.%'` 仅保留为兜底 sanity check。
4. 新增任何含 `amount_cents` 的类型而未登记本表 = 契约违规，W1 后按 schema 演进流程处理。

### 1.4 旧名 → 契约名对照（01/03/04 修订时逐条替换）

| 旧名（出处） | 契约名 |
|---|---|
| `payroll`（01 §6.1）、`world.payday`（03 §5.3） | `economy.payroll` |
| `bill.rent` / `bill.utility`（01 §6.1） | `economy.bill.rent` / `economy.bill.utility` |
| `stock.tick`（01 §6.1）、`world.stock`（03 §5.3） | `economy.stock.tick` |
| `announce`（01 §6.1） | `world.announce` |
| `perf.review`（01 §6.1） | `world.perf_review` |
| `promotion.window`（01 §6.1） | `world.promotion_window` |
| `team_building` / `holiday`（01 §6.1） | `world.team_building` / `world.holiday` |
| `company.crisis`（01 §6.1） | `world.company_crisis` |
| `disturb.illness/.weather/.complaint/.lucky`（01 §6.1） | `world.disturb.*`（原名保留为 object.verb 段） |
| `appointment.created/.remind/.stood_up`（01 §5.3） | `social.appointment.*` |
| `dialogue`（04 §5.2/§6.4/§10.1） | `dialogue.chat` |
| `invite.*`（04 §5.2 注释） | `social.invite` / `social.invite.counter` |
| `agent.chat` / `agent.gossip` / `agent.argue`（03 §5.3/§3.4） | `dialogue.chat` / `dialogue.gossip` / `dialogue.argue` |
| `agent.invite` / `agent.refuse`（03 §5.3） | `social.invite` / `social.refuse` |
| `sim.day_summary`（03 §5.3） | `time.day_summary` |
| `time.catchup_{start,end}`（04 §3.2） | `time.catchup.start` / `time.catchup.end` |
| `llm.failover` / `llm.throttle_*`（04 旧版 §8.2/§8.4，已全量替换） | `system.llm.failover` / `system.llm.throttle` |
| `move`（04 §10.1 审计②） | `agent.move` |
| 18 动作名（01 §4，动作层不变） | 事件层按上表域归属：move/work/rest/eat/shop/trade_stock/think → `agent.*`；chat/argue/gossip/confess/apologize → `dialogue.*`；send_message/invite/give_gift/help/borrow_money/repay_money/refuse → `social.*` |

---

## 2. 跨文档字段对照表

| 字段 | 唯一定义处 | 类型 / 形态 | 使用方与修正点 |
|---|---|---|---|
| `agent_id` | 01 §2.2 | **TEXT，`'A01'~'A40'`**，全场景同形 | DB 全表（04 DDL 的 `agents.id`/`actors[]`/`memories.agent_id`/`relations.a_id,b_id` 等 INT 一律改 TEXT）；YAML 人设；03 协议（`"ag07"` → `"A07"`）；UI |
| 事件 id | 04 §5.2 | **DB 主键 `seq BIGINT`**（IDENTITY；副本库唯一索引=幂等位点）；叙事/日志/协议引用形态 **`'e<seq>'`**（如 `e1102`） | 03 `event_id` 字符串统一为 `'e<seq>'` 形态；排序/续传以 `tick` 为准的规定不变（03 §5） |
| `payload.caused_by` | 本表（03 §3.4 提出） | `'e<seq>'`，单值 | 裁决管道写入"由事件直接触发"的因果链；03 涟漪 SQL 第 4 段；**01 §4.1 的 `parent_event_id` 废弃** |
| `payload.cites` | 本表（03 §3.4 提出） | 记忆 id **列表**（`memories.id` BIGINT 数组） | gossip 事件强制引用本人记忆（01 §4.1"不许无中生有"）；03 涟漪递归；**01 的 `content_ref`（单数）废弃** |
| `ui.grade` | 04 §5.1（`ui` JSONB）+ 本表 | `'A'/'B'/'C'`，可空 | 判定标准 01 §6.4（规则式可编程）；赋值 = 裁决管道 step5 后自动打分 + 编剧 K3 复核终审（评审 P1-11）；03 过滤与切片选题；04 §10.2 A 级间隔；**03 协议顶层 `grade` 字段废弃，移入 `ui.*`** |
| `ui.*`（展示字段族） | 04 §5.1；视觉 token 见 02 §7 | `events.ui` JSONB | UI 展示字段统一收在 `ui.*` 下（grade、气泡样式、镜头提示、切片标记）；03 协议事件形态同步 |
| `payload.lines[].at_offset_s` | 本表（源方案 §3.4） | float 秒，相对整段起点 | 对话逐句播放唯一时间表；**客户端倍率为唯一节拍权威**；04 `presentation_queue` 表与云端 play_at worker 取消（评审 P2-7） |
| `memories.is_witness` | 本表（03 §3.4 提出） | BOOLEAN，目击投影 = true | 04 DDL 补列；03 涟漪 SQL 第 1 段；副本库侧由 05 物化表 `memory_projection` 承接 |
| `memories.source_event_seq` | 04 §5.2 | BIGINT = `events.seq` | 双人事件单源与目击投影的指针；**03 的 `source_event_id` 与本字段同义，统一更名为 `source_event_seq`**（值是 seq 不是 `'e<seq>'` 字符串） |
| `payload.text_display` / `payload.text_raw` | 03 §7 / 04 §11.2 | 展示通道（可出站）/ 内部通道（永不出站） | 副本库物理无 `text_raw` 列（白名单同步）；digest 以同步规范形计算（00 §跨文档衔接点）；05 物化表只含白名单列 |
| `payload.amount_cents` | 本表 | BIGINT，单位分，正入负出 | §1.3 economy 对账规则的统一金额字段；04 审计① 按此求和（修 `(v->>'amount_cents')` 引用） |

> 副本库物化表清单（`events`（白名单）/`memory_projection`/`relation_change_log`/`relation_daily`/`health_daily`/`world_state_snapshot`/`ripple_edge`）为 05 的契约名，本表不复制定义。

---

## 3. 数字登记表

引用方只写"见持有方"，**不得复制数值**；持有方改数必须同步全部引用方与本表。

| 数字项 | 取值 / 口径 | 唯一持有方 | 引用方 |
|---|---|---|---|
| 五指标阈值（A 级事件间隔 / 事件类型熵 / 出场基尼 / 对话 3-gram 重复度 / 关系图周变化率） | 见 01 §9 阈值表（熵 2.4/2.0、基尼 0.45/0.6、3-gram 8%/12%、周变化率 8~25% 与 5%/40% 等） | **01 §9** | 03 §3.6（改为引用，不另持阈值）、04 §10.2（改为引用）、源方案 §8.3（只持指标名，保持） |
| 事件量估算 | **"W2 实测回填"**（8 人小世界实测后由 04 §5.2 写死；此前任何"1500~2500/模拟日""2~4 万/真实日"均为占位口径，不作预算依据） | 04 §5.2（W2 回填） | 03 §4.4（seek/环缓冲预算标注"W2 实测回填"）、04 §9.2 追平吞吐 |
| 对话轮数 | 6~8 轮 | 源方案 §3.4/§4.7 | 01 §7、03 §3.1/§9.1、04 §6.4 |
| 干预率 KPI | <15%（7 日滑动窗口） | 源方案 §4.8 | 01 §6.3、03 §3.6、04 §1.6/§10.1 |
| 成本熔断线 | 实测基线的 1.5× 报警 / 2× 自动降速 | 源方案 §5.2 | 04 §8.4/§1.6、03 §3.6 |
| gossip 传播深度 | 上限 **4 手** | 01 §4.1 | 03 §3.4（递归 `hop<6` 改为 `hop<=4`）、05 `ripple_edge` |
| LOD 三层人数 | 明星 8 / 次要 ~12 / 背景 ~20 | 源方案 §3.2 | 01 §2.3、04 §4 |
| 认知调用量 | 明星 ~96 次/模拟日/人、次要 ~144 合计、背景 ~40 合计 | 源方案 §3.2/§5.2 | 03 §4.4、04 §8.1/§13 |
| A 级事件 KPI | 每模拟日 ≥1 个 | 源方案 §4.8 | 01 §6.4/§9、04 §10.2 |
| 邀约接受率先验 | 40~60%（调参指标非硬改判） | 源方案 §4.7 | 01 §3.5 |
| 项目止损线 | ¥8,000 预算线；评估界 = 门禁③数据收集完成 + 2 周且最迟 14 周；单价上限 W2 写死 | 源方案 §7.3 | 04 §1.6（`WSIM_COST_LIMIT_CNY_PER_SIMDAY`） |
| 切片节奏 | 每周 3 条 × 4 周（12~15 条） | 源方案 §7.1/§8.1 | 01 §8.2/§8.3 |
| 还款期限 | 14 模拟日（`social.repay_money` 配套，P0-2） | 01 §3.2/§4 | 04 debts 表 DDL |

---

## 4. 仲裁条款

1. **数值口径**：与源方案冲突时以源方案为准；**源方案未定义的数值以 01 为准**；01 修订数值必须同步全部引用点与本表（否则以本表登记值为准回改）。
2. **命名与字段**：事件类型、字段名、ID 形态以本表为唯一仲裁方；W1 Schema v1 冻结（仓库 tag `schema-v1`）前，01/03/04 须逐行核对本表 §1.4 与 §2 的修正点。
3. **引用纪律**：各文档引用本表条目时写"见 06 §x"，不复制全表、不另持数值；评审发现的"三套方言"类问题回改后，以本表为回归核对清单。
4. **本表自身的修订**：属契约变更，须记开源方案"附：确认项记录"；新增事件类型/域、改字段名、改已冻结数值三者都算契约变更。
